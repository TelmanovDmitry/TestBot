bot_name=VAIEchoTestBot
bot_key=5895673300:AAGKQWTstfQ9W5lXqxggY8v5o4BFb643JDE
default_delay=2000

# Resieve update and etc requests
spring.mvc.hiddenmethod.filter.enabled=true

#db related settings
spring.datasource.url=jdbc:postgresql://localhost:5432/VAIEchoBot
spring.datasource.username=postgres
spring.datasource.password=admindima
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# = JPA / HIBERNATE
# ===============================
# Show or not log for each sql query
spring.jpa.show-sql=true
# Hibernate ddl auto (create, create-drop, update): with "create-drop" the database
# schema will be automatically created afresh for every start of application
spring.jpa.hibernate.ddl-auto=create-drop

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect